## 1.4.0

- Upgraded Flutter SDK from version `>=3.20.0` to `>=3.32.5`.
- Upgraded the Dart SDK from version `>=3.0.0` to `>=3.8.0`.
- Code cleanup and refactoring due to the Dart SDK upgrade.

## 1.3.1

- Fix to properly dispose the `LocalParticipant` tracks and media devices when the `Room` **disconnected** event is triggered.

## 1.3.0

- Upgraded Flutter SDK from version `>=1.17.0` to `>=3.20.0`.
- Dependency bumps to fix vulnerabilities.
  - Upgraded `twilio_video` in Android SDK from version `6.4.+` to `7.5.1`.
